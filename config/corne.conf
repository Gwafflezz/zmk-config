# RGB per-key: driver ligado, fita apagada no boot; acende com &rgb_ug RGB_TOG
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_UNDERGLOW_ON_START=n
CONFIG_ZMK_RGB_UNDERGLOW_BRT_START=60
CONFIG_ZMK_RGB_UNDERGLOW_EFF_START=0
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE=y
# Sem acoplar o ext power ao RGB (ver CONFIG_ZMK_EXT_POWER abaixo).
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n

# Enable Corne OLED Display & Battery Status
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE=y
# O OLED acende no flash (bootloader, P0.13 solto) e apaga assim que o ZMK assume, nas
# duas metades: o controle de ext power do nice_nano_v2 (P0.13 GPIO_ACTIVE_HIGH) nao
# corresponde ao MOSFET de VCC do clone SuperMini. Sem o driver, o ZMK nao toca no pino
# e o VCC fica como o bootloader deixa -- alimentado. Custo: sem corte de VCC no deep
# sleep (os WS2812 ficam com corrente de repouso).
CONFIG_ZMK_EXT_POWER=n
# Descartando tambem o blank de idle enquanto o OLED nao estabiliza; voltar para y
# (default do SSD1306) depois que estiver confirmado, para economizar bateria.
CONFIG_ZMK_DISPLAY_BLANK_ON_IDLE=n

# Power Management & Deep Sleep
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_TIMEOUT=30000
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# Enable mouse emulation
CONFIG_ZMK_POINTING=y

# Live layer events over Raw HID for the Corne layer overlay (tools/).
CONFIG_ZMK_KEYPEEK_LAYER_NOTIFIER=y
CONFIG_ZMK_STUDIO=y
