# Uncomment the following lines to enable the Corne RGB Underglow
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_UNDERGLOW_ON_START=n
CONFIG_ZMK_RGB_UNDERGLOW_BRT_START=0

# Uncomment the following line to enable the Corne OLED Display
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=y

# Enable mouse emulation
CONFIG_ZMK_POINTING=y

# Live layer events over Raw HID for the Corne layer overlay (tools/).
# The KeyPeek layer notifier is default-y on the central half and selects
# RAW_HID automatically; set explicitly to be safe.
CONFIG_ZMK_KEYPEEK_LAYER_NOTIFIER=y
CONFIG_ZMK_STUDIO=y
